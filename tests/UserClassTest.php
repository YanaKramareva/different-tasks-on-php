<?php

namespace App\Tests;

use App\User;
use PHPUnit\Framework\TestCase;

/*class UserClassTest extends TestCase
{
    public function testAddresses()
    {
        $user = new User('Ivan');
        $address = new User\Address('Russia', 'Moscow', 'Lenina');
        $user->addAddress($address);

        $user2 = new User('Mila');
        $user2->addAddress($address);

        $this->assertEquals(
            $user->getAddresses(),
            $user2->getAddresses()
        );

        $address->setCountry('USA');

        $this->assertEquals(
            $user->getAddresses(),
            $user2->getAddresses()
        );

        $adress2 = new User\Address('Russia', 'Omsk', 'Belinskigo');
        $user->addAddress($adress2);
        $this->assertIsArray($user->getAddresses());
        $this->assertCount(2, $user->getAddresses());
    }
}
*/
