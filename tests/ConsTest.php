<?php

namespace App\Tests;

use PHPUnit\Framework\TestCase;

use function App\Solution\cons;
use function App\Solution\car;
use function App\Solution\cdr;

/*class ConsTest extends TestCase
{
    public function testCar()
    {
        $firstNum = rand();
        $secondNum = rand();

        $pair = cons($firstNum, $secondNum);

        $this->assertEquals($firstNum, car($pair));
    }

    public function testCdr()
    {
        $firstNum = rand();
        $secondNum = rand();

        $pair = cons($firstNum, $secondNum);

        $this->assertEquals($secondNum, cdr($pair));
    }
}
*/
